### 代码

``` java
import java.util.*;

class Solution {
    public int canCompleteCircuit(int[] gas, int[] cost) {
        int n = gas.length;
        /*for (int i = 0; i < n; i++) {
            int sum = 0, j = i;
            boolean flag = true;
            while ((j + 1) % n != i) {
                sum += gas[j];
                if (sum < cost[j]) {
                    flag = false;
                    break;
                }
                sum -= cost[j];
                j = (j + 1) % n;
            }
            sum += gas[j];
            if (sum < cost[j])
                flag = false;
            if (flag)
                return i;
        }*/
        int sum = 0, i = 0, ans = 0;
        boolean flag = false;
        while (i < 2 * n) {
            if (i == ans + n) {
                flag = true;
                break;
            }
            int j = i % n;
            sum += gas[j];
            if (sum < cost[j]) {
                ans = i + 1;
                sum = 0;
            }
            else {
                sum -= cost[j];
            }
            i++;
        }
        return flag ? ans : -1;
    }
}
```

